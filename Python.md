Generic Python Environment setup instructions

## Setup Dependencies
```bash
# pip3 or pip
pip3 install --user uv #ruff
cd /my/git/proj/dir/
source .venv/bin/activate
python3 -m uv venv -p 3.11.7
```
